name: CI/CD Multiarch Portfolio Devop

on:
  push:
    branches:
      - develop

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push multiarch image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: roxsross12/portfolio-devop:develop-${{ github.sha }}
          platforms: linux/amd64,linux/arm64

      - name: Checkout GitOps repository
        uses: actions/checkout@v4
        with:
          repository: roxsross/roxs-gitops-devops
          token: ${{ secrets.GITOPS_REPO_TOKEN }}
          path: gitops-repo

      - name: Update image tag in ArgoCD Helm values
        run: |
          cd gitops-repo/argocd-helm
          sed -i "s|tag:.*|tag: develop-${{ github.sha }}|g" values.yaml

      - name: Commit and push changes to GitOps repo
        run: |
          cd gitops-repo
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "ci: update portfolio-devop image to develop-${{ github.sha }} [skip ci]"
          git push origin main